class StoreData {
  final String name;     // 가게 이름
  final String address;  // 가게 주소
  final String distance; // 사용자 위치로부터의 거리

  StoreData({
    required this.name,
    required this.address,
    required this.distance,
  });
}